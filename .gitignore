# Build output
/target
Cargo.lock.orig
*.swp

# Secrets
.env

# OS
.DS_Store

# Node
node_modules/
bridge-worker/.wrangler/

# Run artifacts — episode records are regenerated by `janus batch`.
# Curated findings are promoted into results/, which IS tracked.
/reports
