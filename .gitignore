# Build output
/target
Cargo.lock.orig
*.swp

# Secrets
.env

# OS
.DS_Store

# Node
node_modules/
bridge-worker/.wrangler/

# Scratch run output. `results/runs/` is the archive and IS tracked —
# episode records are verbose JSON and git packs them ~15x, so full recall
# costs a few MB rather than the 97MB the working tree suggests.
/reports
